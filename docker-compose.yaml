version: '3.8'

services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bot_container
    env_file:
      - .env
    volumes:
      - .:/app
    restart: always

